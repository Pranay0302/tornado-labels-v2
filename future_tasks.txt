- chm and ndvi work 

- create a new branch and Build a minimal and aesthetic Python GUI for inspecting GeoTIFF orthomosaics using Rasterio. The user should be able to upload a .tif or .tiff file and automatically view the raster width, height, CRS, resolution, bounds, transform, NoData value, number of bands, band descriptions, data types, and basic statistics for each band and few more important details. Add band selection and visualization so the user can inspect individual bands or RGB composites. Since the tiling process is already implemented, integrate with the existing tiling logic and allow the user to enter or view the tile size and overlap, then calculate and display the expected number of tiles along the X and Y directions and the total tile count before running the tiling process. Include an orthomosaic preview with the tile grid overlaid and an option to preview individual tiles. Use windowed or downsampled reads so very large GeoTIFFs are not loaded entirely into memory, and keep the implementation modular and easy to connect to the existing tiling code.

- Make the annotation operation work properly, which one is better SAM or DINOv3? if we select one part of the image for example tree damage, it should auto label other parts as well. (this should be done in roboflow)
